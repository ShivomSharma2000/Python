# Create a lambda function to compute the cube of a number
cube = lambda num: num ** 3
print(cube(3))

#anotherCube = cube   # we can't assign lambda function value to any other variable.
# print(anotherCube)